# reciptapp-mobile

Expo + TypeScript scaffold for the ReciptApp mobile frontend.

Quick setup:

```bash
cd mobile
npm install
npx expo prebuild  # optional for native builds
npm run start
```

Notes:
- Expo SDK pinned to ~54.0.0 as requested.
- Install dependencies in `mobile/` before running. If you want, I can run installs for you.
